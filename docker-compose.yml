services:
  mongodb:
    image: mongo:7
    container_name: lorawan_mongodb
    ports:
      - "27017:27017"

  app:
    build: .
    container_name: lorawan_pipeline
    depends_on:
      - mongodb
    environment:
      MONGO_URI: mongodb://mongodb:27017/
    volumes:
      - ./output:/app/output
